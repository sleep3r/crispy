[package]
name = "virtual_mic_ipc"
version = "0.1.0"
edition = "2021"
description = "Shared memory IPC for Crispy virtual microphone"

[dependencies]
libc = "0.2"

[lib]
crate-type = ["staticlib", "rlib"]
